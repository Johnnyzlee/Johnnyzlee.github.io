# jemdoc: nofooter, nodefaultcss, noeqs, addcss{../blog-note}, addjs{../math-render}, addjs{../blog-sidebar}
= World Models: A Beginner's Learning Note
[../../blog.html ← 返回 Blogs] · /更新于 2026-07-26/

~~~
{}{raw}
<div class="page-lead"><strong>为什么学习 World Models？</strong>我希望理解一种比“看到输入就给出输出”更接近完整智能体的思路：系统能否形成对环境的内部表征，预测“如果采取某个动作，接下来可能发生什么”，并据此规划行动？本文从零开始建立概念地图，不假设读者已经熟悉 reinforcement learning。</div>
<div class="study-callout"><strong>先记住一句话：</strong>World Model 不是某一种固定网络，而是一种功能角色——它在智能体内部近似环境的状态与动态，让智能体能够在真正行动前进行预测、反事实比较或 imagined rollout。一个实用的判断标准是：模型能否回答“现在处于什么状态”“采取动作后会怎样”“这个结果是否值得”。</div>
<p class="tex-fallback-note" role="status">MathJax 未能加载时，公式将以 TeX 源码显示。</p>
~~~

== 1. World Model 到底是什么？

在 sequential decision making 中，智能体不断接收 observation，选择 action，并获得新的 observation 与 reward。World Model 是智能体从经验中学到的环境近似模型。它不必复刻世界的每一个像素，只要保留对预测和决策重要的规律。

一个 World Model 通常承担以下部分或全部功能：

- *State representation*：把当前 observation 与历史压缩为 latent state。
- *Dynamics prediction*：预测采取 action 后，latent state 如何变化。
- *Outcome prediction*：预测 reward、episode 是否继续、成功概率或其他任务相关量。
- *Simulation*：生成一条或多条可能的未来轨迹。
- *Support for planning*：让 planner、policy、actor 或 tree search 利用这些预测选择 action。

因此，“生成一段看起来真实的视频”和“拥有可用于行动的 World Model”并不完全等价。前者重视视觉逼真度；后者还需要 action-conditioned prediction、时间一致性，以及对决策有用的正确性。学术文献对 World Model 的边界并没有唯一标准，阅读论文时应先问作者究竟让模型预测什么、又怎样使用预测。

=== 1.1 最小数学表达

设第 +t+ 步的 observation、action、reward 和 latent state 分别记为 +o_t+、+a_t+、+r_t+、+z_t+。一个常见的 latent World Model 可以写成：

~~~
{}{raw}
<div class="math-display">$$
\begin{aligned}
z_t &amp;\sim q_\phi(z_t\mid o_{\le t},a_{\lt t}) &amp;&amp; \text{state inference},\\
\hat z_{t+1} &amp;\sim p_\phi(z_{t+1}\mid z_t,a_t) &amp;&amp; \text{dynamics},\\
\hat r_t &amp;\sim p_\phi(r_t\mid z_t,a_t) &amp;&amp; \text{reward prediction},\\
\hat c_t &amp;\sim p_\phi(c_t\mid z_t,a_t) &amp;&amp; \text{continuation prediction}.
\end{aligned}
$$</div>
~~~

+q_phi+ 根据已经看到的历史推断当前状态，+p_phi+ 则在没有未来 observation 的情况下向前预测。若需要可视化，还可加入 decoder，从 +z_t+ 重建 +o_t+；但 decoder 并不是 World Model 的必要条件。

给定候选 action sequence，planner 可以在模型里比较未来回报：

~~~
{}{raw}
<div class="math-display">$$
a_t^*
=\arg\max_{a_{t:t+H-1}}
\mathbb E_{\hat z,\hat r}
\left[\sum_{k=0}^{H-1}\gamma^k\hat r_{t+k}\right].
$$</div>
~~~

这里的 +H+ 是 imagination horizon，+gamma+ 是 discount factor。真正执行时通常只采用当前的第一个 action，收到新 observation 后重新估计状态并再次规划；这就是 Model Predictive Control（MPC）的基本思想。

== 2. 为什么不直接学习 Policy？

Model-free RL 可以直接从经验学习 policy 或 value function，而不显式学习环境怎样变化。这条路线通常结构更直接，却可能需要大量真实交互。World Model 增加了一个中间层：先从经验中学习可复用的动态规律，再用模型产生更多 imagined experience 或执行 planning。

~~~
{}{raw}
<table>
<thead>
<tr><th>路线</th><th>核心问题</th><th>典型优势</th><th>典型风险</th></tr>
</thead>
<tbody>
<tr><td>Model-free RL</td><td>在当前状态直接选什么 action？</td><td>不必维持一个准确模拟器；流程相对直接</td><td>真实交互通常昂贵；学到的知识未必容易复用</td></tr>
<tr><td>Model-based RL / World Model</td><td>action 会怎样改变未来？怎样利用预测决策？</td><td>可在模型内规划和训练，通常更节省环境交互</td><td>模型误差会被 policy 或 planner 放大</td></tr>
<tr><td>Video generation</td><td>未来画面怎样看起来合理？</td><td>能学习丰富的视觉与时序模式</td><td>视觉逼真不保证物理正确、可控或适合 planning</td></tr>
<tr><td>Language model</td><td>下一个 token 的条件分布是什么？</td><td>包含大量语言化的世界知识</td><td>仅凭 next-token prediction 不能自动证明其具有稳定、可行动的物理 World Model</td></tr>
</tbody>
</table>
~~~

World Model 的主要吸引力是 *data efficiency*：真实机器人试错、自动驾驶事故、科学实验和长时间游戏交互都很昂贵，而神经网络内部的 rollout 可以并行且高速。但 imagined data 不是免费真相；它只是模型当前相信的未来。

== 3. 一次完整的学习与行动循环

把不同论文的细节暂时拿掉，World Model agent 通常重复四步：

. *Collect experience*：在真实环境中收集 +o_t+、+a_t+、+r_t+ 与下一步 observation。
. *Learn the model*：训练 encoder、dynamics、reward／continuation heads，或其他任务相关预测头。
. *Imagine or plan*：从真实经验推断出的 latent state 出发，在模型内展开候选未来。
. *Act and correct*：执行 action，观察真实结果，把新数据加入 replay buffer，再修正模型与行为。

这里最重要的闭环是“预测—行动—观测—纠错”。一个只会离线生成未来、却不能根据新 observation 更新 belief 的模型，在 partially observable 或不断变化的环境中很容易漂移。

=== 3.1 三种常见的用模方式

- *Online planning*：每一步临时搜索 action sequence，例如 PlaNet 使用 latent dynamics 配合 MPC。
- *Policy learning in imagination*：在 imagined trajectories 上训练 actor 与 critic，执行时直接由 actor 出 action，例如 Dreamer。
- *Tree search over a learned model*：模型预测搜索需要的量，再用 tree search 决策，例如 MuZero。

这三种方法没有绝对优劣。Online planning 灵活但推理成本较高；训练好的 policy 执行快，但行为会受训练分布限制；tree search 适合离散 action 和可系统探索的分支，却可能非常耗算力。

== 4. 关键工作串成一条主线

=== 4.1 World Models：Vision、Memory、Controller

[https://worldmodels.github.io/ Ha and Schmidhuber (2018)] 将 agent 分为三个组件：

- *Vision (V)*：VAE 把图像压缩为 latent vector +z_t+。
- *Memory (M)*：MDN-RNN 根据 +z_t+、+a_t+ 与 hidden state +h_t+ 预测下一 latent state 的分布，用概率形式表达随机未来。
- *Controller (C)*：一个刻意保持很小的 controller，根据 +z_t+ 与 RNN hidden state +h_t+ 选择 action。

这项工作的直觉非常有感染力：agent 可以在内部生成的 “dream” 中训练 controller，再把 policy 放回真实环境。它也暴露了一个至今仍核心的问题——controller 可能找到模型漏洞，而不是真正解决任务。原文通过提高 sampling temperature，使 dream 更不确定、更难，以降低 policy 利用模型缺陷的机会。

=== 4.2 PlaNet：直接在 Latent Space 规划

[https://arxiv.org/abs/1811.04551 PlaNet (2019)] 学习同时包含 deterministic 与 stochastic components 的 Recurrent State-Space Model（RSSM），并在 compact latent space 中做 online planning。它的重要贡献不是生成更漂亮的未来图像，而是让从 pixels 出发的多步预测足够支持控制。

初学时可以把 RSSM 理解为两种记忆的结合：deterministic recurrent state 保存历史上下文，stochastic state 表达当前仍有多种可能的环境状态。二者共同处理 partial observability 与 stochasticity。

=== 4.3 Dreamer：在想象中训练行为

[https://arxiv.org/abs/1912.01603 Dreamer (2020)] 沿用 latent dynamics，但不再为每个真实 action 都运行昂贵的 online planner。它从 replayed experience 学习 World Model，再在 latent imagination 中训练 actor 和 critic，最后让 actor 到真实环境执行。

[https://www.nature.com/articles/s41586-025-08744-2 DreamerV3] 进一步通过 normalization、balancing 与 transformed losses 等稳定化设计，以固定 hyperparameters 覆盖 150 多个不同任务。该论文于 2025 年发表于 /Nature/；论文报告 DreamerV3 能从 pixels 与 sparse rewards 出发，在没有 human data 或 curriculum 的情况下于 Minecraft 收集 diamond。这个结果说明 World Model 不只是一种漂亮的解释框架，也可以成为强大的通用 RL algorithm；但它仍是在明确 benchmark 与 action space 下得到的实验结果，不等于已经获得通用现实世界理解。

=== 4.4 MuZero：只预测 Planning 真正需要的量

[https://www.nature.com/articles/s41586-020-03051-4 MuZero (2020)] 提出了一个非常重要的反例：World Model 不必重建 observation，也不必学会一个人类可解释的完整 simulator。MuZero 学习可反复展开的 latent model，预测 reward、policy 与 value，再结合 Monte Carlo Tree Search。

这引出一个核心设计问题：模型应该尽量完整地重建世界，还是只保留与任务相关的 sufficient information？完整模型可能更通用，但浪费容量在无关细节；task-oriented model 更高效，却可能在更换任务后丢失原先没有预测的关键信息。

=== 4.5 JEPA 与 V-JEPA 2：预测 Representation，而非每个 Pixel

[https://openreview.net/forum?id=BZ5a1r-kVsf LeCun (2022)] 提出的 JEPA 路线强调在 representation space 预测。其直觉是：未来存在许多不可预测的像素细节，模型没有必要为每片树叶的精确位置分配能力，应该优先学习语义与物理上可预测的部分。

[https://arxiv.org/abs/2301.08243 I-JEPA (2023)] 先在静态图像中验证 joint-embedding prediction；[https://arxiv.org/abs/2506.09985 V-JEPA 2 (2025)] 则从大规模视频进行 self-supervised pretraining，再用少量 robot interaction data 训练 action-conditioned predictor。论文报告 V-JEPA 2-AC 可配合 MPC，在新环境中进行 image-goal robot planning。这里应区分两个阶段：普通视频主要提供观察世界的经验，而 action-conditioned interaction data 才把“看见变化”连接到“我的 action 会造成什么变化”。

=== 4.6 Genie：可交互的 Generative Environment

[https://deepmind.google/research/publications/60474/ Genie (2024)] 从没有 action labels 的 Internet videos 学习 latent actions，并生成可逐帧控制的环境。它把 World Model 与 generative video 的路线明显拉近。

[https://deepmind.google/blog/genie-3-a-new-frontier-for-world-models/ Google DeepMind 对 Genie 3 的官方介绍]称其可以根据 text prompt 生成 720p、24 FPS、可实时导航并在数分钟内保持一致的动态环境。这个方向的目标不仅是控制一个已知 benchmark，而是生成可供其他 agents 学习的丰富环境。需要注意：Genie 3 的公开信息主要来自官方 system announcement，不能把演示效果直接当作完整、独立验证过的物理模拟能力。

== 5. 理解不同 World Model 的三个坐标轴

与其争论某个系统“算不算” World Model，不如沿三个坐标轴比较。

~~~
{}{raw}
<table>
<thead>
<tr><th>坐标轴</th><th>一端</th><th>另一端</th><th>阅读时要问</th></tr>
</thead>
<tbody>
<tr><td>Prediction target</td><td>pixels／observations</td><td>latent features／reward／value</td><td>模型保留了什么，又主动忽略了什么？</td></tr>
<tr><td>Conditioning</td><td>观察式 prediction</td><td>action-conditioned counterfactual prediction</td><td>它能否区分“世界自己变化”和“action 导致变化”？</td></tr>
<tr><td>Decision mechanism</td><td>online search／MPC</td><td>policy learned in imagination</td><td>计算成本发生在训练时还是执行时？</td></tr>
</tbody>
</table>
~~~

还可以增加第四个坐标轴：模型是针对单个环境与任务训练，还是试图成为跨环境的 foundation world model。越靠近后者，数据覆盖、状态抽象、长期记忆和 evaluation 都会变得更困难。

== 6. World Models 最难的地方

=== 6.1 Compounding Error

One-step prediction 很准，不代表连续 rollout 很准。预测状态一旦偏离 training distribution，下一步又以错误状态为输入，误差会逐步放大。评估时必须同时看 one-step loss、multi-step rollout 和最终 control performance。

=== 6.2 Model Exploitation

Planner 或 policy 会主动搜索能获得高 predicted reward 的轨迹，因此它比普通测试样本更容易撞到模型盲区。模型可能预测“穿墙”“无限奖励”或不真实的稳定状态，agent 却把它当捷径。这是 optimization pressure 对 model error 的放大。

=== 6.3 Stochasticity 与 Multimodality

未来经常不唯一。若模型只用 mean-squared error 预测一个平均未来，可能得到现实中根本不存在的模糊状态。概率分布、discrete latent variables、mixture models 或 diffusion-style prediction 都是在表达多种未来。

=== 6.4 Partial Observability 与 Memory

单帧 observation 往往不足以推断速度、遮挡物或其他 agent 的意图。World Model 必须从历史形成 belief state；同时又要避免让长期记忆被无关细节淹没。

=== 6.5 Causality 与 Controllability

Passive video 告诉模型事件通常怎样接续，却不自动揭示 intervention 的结果。真正用于 planning 的模型需要回答 “如果我采取 action +a+，未来会怎样”，这也是 action data、robot interaction 和 exploration 难以完全省略的原因。

=== 6.6 Uncertainty 与 Out-of-Distribution Generalization

模型应该知道自己何时不知道。Aleatoric uncertainty 来自环境本身的随机性；epistemic uncertainty 来自数据不足与模型无知。若二者都被一个自信的 point estimate 隐藏，planner 会在陌生区域做出危险决定。

=== 6.7 Evaluation

视觉质量、prediction loss、reward prediction 和 task return 衡量的是不同能力。一个可靠的 evaluation 至少应问：

- 预测是否在长 horizon 保持一致？
- action 是否真的改变预测结果，而不是只改变画面表象？
- planner 使用模型后是否优于不用模型的 baseline？
- 是否在 unseen states、unseen objects 或新环境中保持能力？
- 模型失败时能否通过 uncertainty 或新 observation 及时纠正？

== 7. 我目前形成的概念判断

. *World Model 的核心不是生成，而是可用于反事实决策的预测。* 生成 observation 只是其中一种实现。
. *Latent state 不是压缩得越小越好。* 它需要保留对未来与任务足够的信息，同时丢掉不可预测或无关的细节。
. *“在 imagination 中训练”同时带来 data efficiency 和 model bias。* 两者是一枚硬币的两面。
. *Passive observation 很重要，但 action-conditioned data 仍然关键。* 相关性知识必须连接到 intervention 才能可靠支持 control。
. *World Model 不等于通用智能。* 它是 perception、memory、prediction、planning 与 action system 中的重要部件，而不是单独解决所有问题的标签。
. *最可信的证据是闭环行为。* 漂亮的视频或低 prediction loss 都不如在新 observation 持续到来时，模型能否帮助 agent 稳健达成目标。

== 8. 面向初学者的学习路线

=== Stage 0：补齐最少前置知识

先掌握以下概念，不必一次学完所有证明：

- Probability：conditional probability、expectation、KL divergence、sampling。
- Sequential decision making：MDP、POMDP、state、action、reward、return、policy、value。
- RL basics：Bellman equation、model-free 与 model-based、actor–critic。
- Deep learning：VAE、RNN／GRU、Transformer、representation learning。
- Control basics：planning、MPC、open-loop 与 closed-loop。

=== Stage 1：从直觉最强的原始工作开始

阅读 [https://worldmodels.github.io/ World Models interactive article]。先不追求所有公式，只回答四个问题：

. V、M、C 分别接收什么、输出什么？
. 为什么 M 预测的是 distribution，而不是单一的 next latent state？
. controller 如何在 dream 中训练？
. policy 为什么会 exploit imperfect model？

=== Stage 2：建立 Model-Based RL 全景

阅读 [https://arxiv.org/abs/2006.16712 Model-based Reinforcement Learning: A Survey] 的 introduction、dynamics model learning 和 planning integration 部分。重点画出两张图：World Model 怎样学习；学到以后怎样与 planning／policy learning 结合。

第一个小实验不要直接复现 DreamerV3。可以从 deterministic gridworld 开始：

. 收集 state、action、next state 与 reward。
. 学一个 tabular transition model。
. 用模型 rollout 比较 action sequences。
. 改成 stochastic transition，观察 point prediction 为什么失败。
. 故意删除一部分数据，观察 planner 如何 exploit 错误模型。

=== Stage 3：学习 Latent Dynamics

按顺序读 [https://arxiv.org/abs/1811.04551 PlaNet] 与 [https://arxiv.org/abs/1912.01603 Dreamer]。重点理解 RSSM、representation model、transition model、reward model，以及 online planning 和 actor–critic in imagination 的差别。

=== Stage 4：理解“预测什么”比“怎样预测”更重要

阅读 [https://www.nature.com/articles/s41586-020-03051-4 MuZero]，思考为什么不重建棋盘 pixels 仍然可以 planning。然后对比 Dreamer 的 reconstruction objective：一个追求 task-relevant prediction，一个更强调可复用的 unsupervised representation。

=== Stage 5：进入现代路线

- [https://www.nature.com/articles/s41586-025-08744-2 DreamerV3]：关注跨 domain 稳定性与 scaling，而非死记工程技巧。
- [https://arxiv.org/abs/2301.08243 I-JEPA] 与 [https://arxiv.org/abs/2506.09985 V-JEPA 2]：关注 latent prediction、passive video pretraining 和 action-conditioned post-training。
- [https://deepmind.google/research/publications/60474/ Genie] 与 [https://deepmind.google/blog/genie-3-a-new-frontier-for-world-models/ Genie 3]：观察 generative interactive environments 如何扩展 World Model 的定义，同时保持对公开证据边界的警惕。

=== Stage 6：再运行大型实现

最后再阅读和运行 [https://github.com/danijar/dreamerv3 official DreamerV3 implementation]。此时目标不是立即复现实验最高分，而是能沿数据流定位：

- replay buffer 中保存什么；
- observation 如何变为 latent state；
- imagined trajectory 怎样生成；
- actor 与 critic 的 loss 从哪里来；
- 哪些 tensor 来自真实环境，哪些来自 World Model。

== 9. 阅读论文时使用的检查表

以后每读一篇 World Model 论文，我会固定回答这些问题：

. *Environment*：fully observable 还是 partially observable？deterministic 还是 stochastic？
. *Data*：来自 random policy、expert、Internet video，还是在线 agent interaction？
. *State*：使用原始 observation、deterministic latent，还是 stochastic latent？
. *Prediction target*：next observation、latent feature、reward、value、termination，还是多项组合？
. *Action conditioning*：模型是否显式输入 action？没有 action labels 时如何学习 controllability？
. *Objective*：reconstruction、likelihood、contrastive／joint-embedding、reward prediction 或 value consistency？
. *Decision*：MPC、tree search、actor–critic，还是模型仅用于 representation？
. *Uncertainty*：模型如何表达多种未来与自己的无知？
. *Evaluation*：只测 prediction，还是闭环 planning／control？是否有 unseen environment？
. *Failure mode*：compounding error、model exploitation 与 distribution shift 如何处理？

== 10. 术语速查

- *Observation*：传感器或环境直接给出的输入，不一定包含完整真实状态。
- *Latent state*：模型内部的紧凑状态表征。
- *Belief state*：在 partial observability 下，对真实状态的概率性估计。
- *Dynamics model*：预测状态随 action 和时间怎样变化。
- *Observation model／decoder*：从 latent state 预测或重建 observation。
- *Reward model*：预测 action 或状态会带来多少 reward。
- *Rollout／imagination*：让模型连续向前生成一条假想轨迹。
- *Planning*：比较可能的未来并选择 action。
- *Policy*：从 state 或 observation 到 action distribution 的映射。
- *MPC*：反复进行短 horizon planning，每次只执行第一个 action，然后用新 observation 重算。
- *Model bias*：学习到的模型与真实环境有系统差异，进而误导决策。
- *Epistemic uncertainty*：因数据或知识不足造成、原则上可通过更多信息降低的不确定性。
- *Aleatoric uncertainty*：环境内在随机性带来的不确定性。

== 11. 参考资料

=== 建议优先阅读

- David Ha and Jürgen Schmidhuber, [https://worldmodels.github.io/ /World Models/], NeurIPS 2018.
- Thomas M. Moerland et al., [https://arxiv.org/abs/2006.16712 /Model-based Reinforcement Learning: A Survey/], /Foundations and Trends in Machine Learning/, 2023.
- Danijar Hafner et al., [https://arxiv.org/abs/1811.04551 /Learning Latent Dynamics for Planning from Pixels/], ICML 2019.
- Danijar Hafner et al., [https://arxiv.org/abs/1912.01603 /Dream to Control: Learning Behaviors by Latent Imagination/], ICLR 2020.
- Julian Schrittwieser et al., [https://www.nature.com/articles/s41586-020-03051-4 /Mastering Atari, Go, Chess and Shogi by Planning with a Learned Model/], /Nature/, 2020.
- Danijar Hafner et al., [https://www.nature.com/articles/s41586-025-08744-2 /Mastering Diverse Control Tasks through World Models/], /Nature/, 2025.

=== 延伸方向

- Yann LeCun, [https://openreview.net/forum?id=BZ5a1r-kVsf /A Path Towards Autonomous Machine Intelligence/], 2022. 这是 position paper，适合了解 JEPA 的研究愿景，不应当作已经完成的系统。
- Mahmoud Assran et al., [https://arxiv.org/abs/2301.08243 /Self-Supervised Learning from Images with a Joint-Embedding Predictive Architecture/], CVPR 2023.
- Mahmoud Assran et al., [https://arxiv.org/abs/2506.09985 /V-JEPA 2: Self-Supervised Video Models Enable Understanding, Prediction and Planning/], 2025.
- Jake Bruce et al., [https://deepmind.google/research/publications/60474/ /Genie: Generative Interactive Environments/], 2024.
- Google DeepMind, [https://deepmind.google/blog/genie-3-a-new-frontier-for-world-models/ /Genie 3: A New Frontier for World Models/], 2025. 这是官方 system announcement，阅读时应与 peer-reviewed paper 分开看待。

本文是第一版概念地图，重点是建立正确问题，而不是覆盖所有 World Model 分支。后续笔记可分别深入 RSSM、Dreamer、MuZero、JEPA、generative environments、robot planning 与 uncertainty-aware planning。
